export async function generateChatbotResponse(attachmentStyle: string, message: string): Promise<string> {
    // Mock response logic for now
    return `Based on your ${attachmentStyle} attachment style, I suggest focusing on open communication.`;
  }
  